package physics;

public class CircleBody extends Body {
    public CircleBody(Vector2 position, Vector2 velocity, double radius, double mass, double restitution) {
        super(position, velocity, radius, mass, restitution);
    }
}